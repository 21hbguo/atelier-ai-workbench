# 项目安全加固建议

## 结论

当前项目已具备基础鉴权、密码哈希、限流与少量响应头，但仍存在几处高风险入口，尤其是任意本地路径缩略图读取、上传文件暴露、客户端可伪造真实 IP、长生命周期 JWT 存储在 `localStorage`。这些问题一旦被利用，可能导致敏感文件探测、存储型 XSS、限流绕过、会话长期失陷。

## 高优先级

### 1. 关闭任意本地路径缩略图读取
问题位置：[backend/routers/images.py](PROJECT_ROOT_security_hardening_wt/backend/routers/images.py:171)

`/api/images/local-thumb` 当前没有登录校验，且直接接收外部传入的任意 `path`。攻击者只要知道服务器上的图片路径，就可以请求生成缩略图并间接读取该文件。

风险：
- 越权读取服务器本地图片
- 暴露用户上传文件、后台素材、运维截图等敏感内容
- 可被用作路径探测接口

建议：
- 立即为该接口增加 `Depends(require_admin)`，至少也要加 `Depends(get_current_user)`
- 禁止接收任意绝对路径，只允许访问 `UPLOAD_DIR`、`GENERATED_IMAGES_DIR` 白名单目录
- 前端改传 `id` 或 `filename`，后端自行查表定位真实文件
- 对目标文件执行 `resolve()` 后做目录前缀校验，防止路径穿越

### 2. 收紧 `/upload/local` 文件类型与访问方式
问题位置：[backend/routers/upload.py](PROJECT_ROOT_security_hardening_wt/backend/routers/upload.py:83)
静态暴露位置：[backend/main.py](PROJECT_ROOT_security_hardening_wt/backend/main.py:61)

`/upload/local` 当前只限制大小，不限制扩展名、MIME、文件头，保存后又通过 `/uploads` 静态目录直接对外暴露。

风险：
- 可上传 `svg`、`html`、伪装脚本文件，形成存储型 XSS 或钓鱼页面
- 支付凭证、用户私有附件被公网长期可访问
- 上传目录可被当作任意文件托管空间使用

建议：
- `upload_local` 只允许 `png`、`jpg`、`jpeg`、`webp`、`pdf`
- 同时校验扩展名、`content-type`、文件头魔数，不只看文件名
- 将 `/uploads` 改成鉴权下载接口，不再静态公开整个目录
- 支付凭证类文件按业务绑定所属用户，下载时校验权限
- 为上传文件生成随机文件名，避免基于时间戳的可预测枚举

### 3. 不要直接信任客户端传入的 `X-Forwarded-For`
问题位置：[backend/auth.py](PROJECT_ROOT_security_hardening_wt/backend/auth.py:116)

`get_client_ip()` 当前只要看到 `X-Forwarded-For` 就直接取第一段 IP。只要请求能直达应用层，客户端就可以自行伪造该头部。

风险：
- 登录、注册、生成、积分等基于 IP 的限流可被绕过
- 用户 `last_ip` 记录不可信，影响审计与风控

建议：
- 默认只使用 `request.client.host`
- 仅在明确部署于受信任反向代理之后时，才解析代理头
- 由 Nginx 显式覆盖转发头，禁止透传客户端自带的 `X-Forwarded-For`
- 如后续有多层代理，需维护可信代理白名单而不是无条件信任

### 4. 缩短 JWT 生命周期，停止存储在 `localStorage`
问题位置：[backend/auth.py](PROJECT_ROOT_security_hardening_wt/backend/auth.py:43)
前端位置：[frontend/src/api.js](PROJECT_ROOT_security_hardening_wt/frontend/src/api.js:10)
[frontend/src/pages/LoginPage.jsx](PROJECT_ROOT_security_hardening_wt/frontend/src/pages/LoginPage.jsx:24)

当前 `JWT_EXPIRE_HOURS` 默认是 `720` 小时，约等于 30 天；前端把 token 放在 `localStorage`，被 XSS 读取后可长期复用。

风险：
- XSS 一旦成立即可直接接管账号
- 用户登出后令牌仍可在其他环境继续使用
- 无刷新机制时通常会被迫把访问令牌做得过长

建议：
- 访问令牌缩短到 `2` 到 `12` 小时
- 增加 `refresh token` 机制，并单独设置更严格的轮换与失效策略
- 改为 `HttpOnly`、`Secure`、`SameSite=Lax` 或 `Strict` Cookie
- 支持服务端撤销机制，例如黑名单、版本号或最近登出时间校验

### 5. 为公开文件访问补齐鉴权与范围控制
问题位置：[backend/main.py](PROJECT_ROOT_security_hardening_wt/backend/main.py:60)
[backend/routers/images.py](PROJECT_ROOT_security_hardening_wt/backend/routers/images.py:208)

当前 `/generated_images`、`/uploads` 以静态目录方式直接挂载，`/api/images/file/{filename}` 也未校验访问者身份与资源归属。

风险：
- 只要猜中或枚举文件名，就可能访问不属于自己的资源
- 后续如果文件名规则稳定，可被批量遍历下载

建议：
- 生成图与上传文件统一改为受控下载接口
- 下载前按 `user_id`、资源归属、管理员权限做校验
- 文件名改为高熵随机值，避免时间戳与原始文件名暴露
- 对外返回短期签名 URL 或一次性下载令牌也可以作为过渡方案

## 中优先级

### 6. 强化安全响应头与 HTTPS 策略
问题位置：[backend/main.py](PROJECT_ROOT_security_hardening_wt/backend/main.py:33)

当前仅设置了 `X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`，还不足以覆盖前端脚本注入、协议降级与浏览器能力滥用等场景。

建议：
- 增加 `Content-Security-Policy`
- 生产 HTTPS 启用后增加 `Strict-Transport-Security`
- 增加 `Permissions-Policy`
- 对静态资源、下载接口区分设置 `Cache-Control`
- 若采用 Cookie 鉴权，再补 `Cross-Origin-Opener-Policy`、`Cross-Origin-Resource-Policy` 评估

### 7. 收紧运行时配置接口暴露
问题位置：[backend/routers/config.py](PROJECT_ROOT_security_hardening_wt/backend/routers/config.py:21)

当前任意已登录用户都可以读取运行时配置，虽然 `api_key` 已打码，但第三方服务地址、图床域名、支付二维码地址等仍会暴露给普通用户。

建议：
- 普通用户接口只返回前端必要字段
- 管理配置读取拆为管理员专用接口
- 对二维码、第三方服务地址等敏感资源做最小化返回

### 8. 为上传与代理下载补充内容校验和资源上限
问题位置：[backend/routers/upload.py](PROJECT_ROOT_security_hardening_wt/backend/routers/upload.py:38)
[backend/routers/images.py](PROJECT_ROOT_security_hardening_wt/backend/routers/images.py:107)

当前图床代理缩略图会主动请求远端资源，本地上传与远程图片处理流程也未体现像素、尺寸、解压后体积等限制。

风险：
- 超大图片、畸形图片可导致内存占用过高
- 图像解析库可能在异常输入下触发稳定性问题

建议：
- 对图片像素总量、边长、帧数、解码后大小设置硬上限
- 代理下载增加响应体大小限制、超时、允许的 `content-type` 白名单
- 对 `proxy-thumb` 的缓存文件数量与生命周期做清理策略

## 部署层建议

### 9. 在 Nginx 层补齐边界防护
文件位置：[nginx.conf](PROJECT_ROOT_security_hardening_wt/nginx.conf:1)

建议：
- 对 `/api/auth/login`、`/api/auth/register`、`/api/generate/` 增加 `limit_req`
- 设置 `client_max_body_size 10m` 或更严格值
- 拒绝危险上传类型的直接访问，例如 `html`、`svg`、`js`
- 明确覆盖 `X-Forwarded-For`、`X-Real-IP`、`Host`
- 生产环境只开放 `80/443`，不要直接暴露应用端口

### 10. 强化密钥与环境变量管理
问题位置：[backend/auth.py](PROJECT_ROOT_security_hardening_wt/backend/auth.py:13)
[.env.example](PROJECT_ROOT_security_hardening_wt/.env.example:23)

当前 JWT 密钥支持自动落盘到 `data/.jwt_secret`，这比每次重启重置更好，但生产环境仍应将密钥、管理员密码、数据库口令纳入统一秘密管理。

建议：
- 生产环境强制从外部秘密管理系统或受控环境变量注入
- 管理员初始密码只允许首次引导时使用，随后强制修改
- 对 `.env`、备份文件、日志输出做脱敏与访问控制
- 建立密钥轮换与失效预案

## 推荐执行顺序

1. 修复 `local-thumb` 越权与任意路径读取
2. 修复 `/upload/local` 文件类型校验与 `/uploads` 静态公开
3. 修复 `get_client_ip()` 的代理信任链
4. 缩短 JWT 生命周期并迁移到 Cookie + refresh token
5. 将公开文件访问改为按资源归属鉴权
6. 补充 CSP、HSTS、Nginx 限流与上传边界控制

## 最低可行加固清单

如果当前只允许做一轮快速修复，建议至少完成以下内容：

1. 封掉 `/api/images/local-thumb` 的匿名任意路径读取
2. 为 `/upload/local` 增加扩展名、MIME、文件头白名单
3. 取消 `/uploads` 静态直出，改为鉴权下载
4. 修正 `get_client_ip()`，不再直接信任客户端代理头
5. 把 JWT 有效期从 `720` 小时降到 `12` 小时以内
