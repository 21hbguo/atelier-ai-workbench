import { Ban, Trash2 } from 'lucide-react'
import SearchInput from '../../components/SearchInput'
import Pagination from '../../components/Pagination'
export default function AdminBannedTab({bannedWordsTotal,bannedWordsQuery,setBannedWordsQuery,setShowBatchImport,newBannedWord,setNewBannedWord,handleAddBannedWord,loading,bannedWords,handleDeleteBannedWord,bannedWordsPage,setBannedWordsPage}){return(<div><div className="flex items-center justify-between mb-4"><div className="flex items-center gap-3 flex-1"><span className="text-sm flex-shrink-0" style={{ color: 'var(--text-secondary)' }}>共 {bannedWordsTotal} 个违禁词</span><SearchInput value={bannedWordsQuery} onChange={setBannedWordsQuery} placeholder="搜索违禁词..." /></div><button onClick={() => setShowBatchImport(true)} className="px-3 py-1.5 rounded-lg text-xs font-medium bg-accent text-white hover:opacity-90">批量导入</button></div><div className="flex items-center gap-2 mb-4"><input type="text" value={newBannedWord} onChange={e => setNewBannedWord(e.target.value)} onKeyDown={e => e.key === 'Enter' && handleAddBannedWord()} placeholder="输入新违禁词..." className="flex-1 px-3 py-2 rounded-lg text-sm border" style={{ borderColor: 'var(--border-color)', background: 'var(--bg-primary)', color: 'var(--text-primary)' }} maxLength={50} /><span className="text-[11px] tabular-nums" style={{ color: 'var(--text-secondary)' }}>{newBannedWord.length}/50</span><button onClick={handleAddBannedWord} disabled={!newBannedWord.trim()} className="px-4 py-2 rounded-lg text-sm font-medium bg-accent text-white hover:opacity-90 disabled:opacity-50 disabled:cursor-not-allowed">添加</button></div>{loading?(<div className="flex justify-center py-20"><div className="w-8 h-8 border-2 rounded-full animate-spin-slow" style={{ borderTopColor: 'var(--accent)', borderColor: 'var(--border-color)' }} /></div>):(<><div className="space-y-2">{bannedWords.map(item=><div key={item.id} className="flex items-center justify-between px-4 py-3 rounded-xl border" style={{ borderColor: 'var(--border-color)', background: 'var(--bg-ai-bubble)' }}><div className="flex items-center gap-3"><span className="px-2 py-1 rounded-lg text-xs font-medium" style={{ background: '#ef444420', color: '#ef4444' }}><Ban size={12} className="inline mr-1" />违禁</span><span className="text-sm truncate max-w-[18rem]" title={item.word} style={{ color: 'var(--text-primary)' }}>{item.word}</span><span className="text-xs" style={{ color: 'var(--text-secondary)' }}>{item.created_at}</span></div><button onClick={() => handleDeleteBannedWord(item.id, item.word)} className="p-2 rounded-lg hover:bg-red-50 dark:hover:bg-red-900/20 text-red-500" title="删除"><Trash2 size={14} /></button></div>)}{bannedWords.length === 0 && (<div className="text-center py-20" style={{ color: 'var(--text-secondary)' }}>暂无违禁词</div>)}</div><Pagination page={bannedWordsPage} totalPages={Math.ceil(bannedWordsTotal / 20)} onPageChange={setBannedWordsPage} /></>)}</div>)}
