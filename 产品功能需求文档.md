# AI 图像生成网站 - 产品功能需求文档

## 1. 产品概述

### 1.1 产品定位
一个轻量级的 AI 图像生成网站，部署在本地服务器上，通过 Tailscale IP 访问。支持纯文本生图和文本+图像生图。

### 1.2 核心说明
- **生成的图**：保存在服务器本地 `generated_images` 文件夹，**不上传图床**
- **图床用途**：仅用于用户上传的参考图，上传后获取 URL 用于提交给 AI 模型
- **去重机制**：一张图只上传一次，通过 CSV 记录本地路径与 URL 的映射
- **双场景**：服务器本地上传 + 用户通过 Tailscale 网站上传

---

## 2. 功能需求

### 2.1 图像生成

#### 2.1.1 纯文本生图
- 输入提示词 → 提交任务 → 轮询结果 → 下载保存到本地
- 可选参数：尺寸、数量、步数、CFG、Seed、模型选择

#### 2.1.2 文本+图像生图
- 输入提示词 + 上传参考图 → 参考图先处理获取 URL → 提交任务 → 轮询结果 → 下载保存

**参考图处理流程**：
```
用户上传参考图
    ↓
检查 CSV 是否已有该图片记录
    ↓
  有 → 直接使用已有 URL
  无 → 上传到图床 → 获取 URL → 写入 CSV
    ↓
将 prompt + 图片 URL 提交给 AI API
```

### 2.2 图片上传与图床管理

#### 2.2.1 两种上传场景

**场景一：服务器本地**
- 图片已在服务器上，直接读取文件上传图床

**场景二：Tailscale 远程访问**
- 用户通过网页上传 → 文件保存到服务器 `uploads/` 文件夹 → 上传图床 → 获取 URL
- 上传的文件统一管理，用文件哈希（MD5/SHA256）或绝对路径去重

#### 2.2.2 CSV 映射文件
- 文件：`image_url_mapping.csv`
- 字段：
  - `local_path`：图片本地绝对路径
  - `url`：图床 URL
  - `upload_time`：上传时间

#### 2.2.3 去重规则
- 上传前先检查 `local_path` 是否已存在于 CSV 中
- 存在 → 直接返回已有 URL
- 不存在 → 上传并追加记录

### 2.3 任务管理
- 异步任务：提交后返回 task_id
- 轮询状态：每 2 秒查询一次，直到成功/失败
- 状态：排队中 → 生成中 → 成功 / 失败
- 失败可重试

### 2.4 提示词仓库

#### 2.4.1 功能说明
- 用户可保存常用提示词，方便后续快速复用
- 提示词持久化保存到服务器本地文件
- 支持批量导入、导出、删除

#### 2.4.2 存储格式
- 文件格式：JSON（推荐）或 CSV
- 文件路径：`data/prompts.json`
- 每条记录结构：
  ```json
  {
    "id": "uuid",
    "name": "提示词名称",
    "prompt": "提示词内容",
    "negative_prompt": "反向提示词（可选）",
    "tags": ["标签1", "标签2"],
    "created_at": "2026-04-24 10:00:00"
  }
  ```

#### 2.4.3 批量导入
- 支持导入 JSON / CSV 文件
- 导入时自动校验格式，跳过重复项（按 prompt 内容或 name 去重）
- 显示导入结果：成功 X 条，失败 X 条

#### 2.4.4 批量导出
- 支持导出为 JSON / CSV 文件
- 可全选或部分勾选后导出

#### 2.4.5 批量删除
- 可多选提示词，批量删除
- 删除前弹出确认提示

#### 2.4.6 其他操作
- 新增提示词：名称 + 内容 + 标签（可选）
- 编辑提示词
- 搜索 / 筛选：按名称、标签、内容搜索
- 一键使用：点击后自动填入生图页面的 Prompt 输入框

### 2.5 图库管理
- 展示 `generated_images` 文件夹中的图片
- 按时间倒序排列
- 点击图片查看详情：原图 + 生成参数（prompt、模型、尺寸等）
- 支持一键复用参数重新生成
- 支持下载
- 支持将当前 prompt 保存到提示词仓库

### 2.6 配置
- `.env` 或 `config.json` 配置：
  - 图像生成 API 地址
  - API Key
  - 图床上传地址
  - 图床域名前缀
- 调用统计：今日/累计次数、成功/失败次数

---

## 3. 非功能需求

- 上传文件格式校验（PNG、JPG、JPEG、WEBP）
- 上传文件大小限制（默认 10MB）
- 频率限制（防止刷接口）
- 支持主流浏览器
- 移动端自适应
- 数据持久化：所有数据（提示词、任务记录、图床映射）保存在服务器本地

---

## 4. 页面结构与交互设计

### 4.1 整体布局（类似 Trae Task / 微信对话）

```
┌─────────────────────────────────────────────────────┐
│ 侧边栏                    │ 主内容区                 │
│ ┌──────────┬──────────────┤                         │
│ │ + 新Task │              │  对话区域（消息流）       │
│ ├──────────┤  选中的Task   │                         │
│ │ Task列表 │  详情/预览    │  - 用户消息（右侧气泡）   │
│ │          │              │  - AI回复（左侧气泡）     │
│ │ - Task 1 │              │  - 生成过程动态展示       │
│ │ - Task 2 │              │  - 生成结果图片           │
│ │ - Task 3 │              │  - 时间戳               │
│ │          │              │                         │
│ │          │              │  底部输入区              │
│ │          │              │  [Prompt输入框] [生成按钮] │
│ └──────────┴──────────────┘                         │
└─────────────────────────────────────────────────────┘
```

### 4.2 侧边栏 - Task 管理

#### 4.2.1 新建 Task
- 点击「+ 新Task」按钮创建新的生图任务对话
- 新建后可输入 Task 名称（可选，默认按时间自动命名）

#### 4.2.2 Task 列表
- 按时间倒序排列
- 显示每个 Task 的：
  - 名称/首条 prompt 摘要
  - 最后活动时间
  - 状态标识（生成中/已完成）
- 点击切换查看不同 Task 的对话记录
- 支持删除 Task（确认后删除整个对话及关联图片）

#### 4.2.3 Task 详情/预览
- 选中 Task 后显示简要信息：
  - 任务数量统计
  - 最新状态

### 4.3 主内容区 - 对话式交互

#### 4.3.1 消息流布局
- **微信对话风格**：一问一答的聊天气泡
  - 用户消息靠右，浅色背景气泡
  - AI 回复靠左，深色背景气泡
- 每条消息显示时间戳

#### 4.3.2 用户消息（右侧气泡）
- 显示用户输入的 Prompt
- 如上传了参考图，显示参考图缩略图
- 显示使用的生成参数（可折叠）

#### 4.3.3 AI 回复（左侧气泡）
- **生成中状态**：动态展示进度
  - 加载动画（旋转圆圈/进度条）
  - 实时状态文字：「正在提交任务...」→「任务排队中...」→「图像生成中...」
  - 每 2 秒自动更新状态
- **生成完成**：
  - 显示生成的图片（可点击放大查看）
  - 显示生成参数摘要
  - 操作按钮：
    - 重新生成（复用参数）
    - 保存到图库
    - 将 prompt 加入提示词仓库
    - 下载图片
- **生成失败**：
  - 显示错误信息
  - 重试按钮

#### 4.3.4 底部输入区
- 模式切换标签：纯文本 / 文本+图像
- Prompt 输入框（支持多行，自动高度）
- **字符统计**：输入框右下角显示当前输入字符数，格式如 `128 字符`
- **参考图上传**（仅文本+图像模式显示）：
  - 点击按钮选择图片上传
  - **支持拖拽上传**：可将图片直接拖入输入区域
  - 拖入时显示半透明遮罩提示「松开鼠标上传图片」
  - 上传后显示图片缩略图预览，可点击 × 移除
  - 支持多张图片（如有需要）
- 参数设置按钮（展开可配置尺寸、步数等）
- 发送/生成按钮

### 4.4 图库页面 (/gallery)
- 独立页面，网格展示所有已保存的图片
- 按时间倒序排列
- 点击图片查看详情

### 4.5 提示词仓库 (/prompts)
- 独立页面
- 提示词列表（卡片/表格）
- 搜索 & 按标签筛选
- 新增 / 编辑提示词
- 批量操作：导入 / 导出 / 删除

### 4.6 任务记录页面 (/tasks)
- 独立页面，表格展示所有 Task
- 可按时间筛选
- 点击查看对应 Task 的对话详情

---

## 5. 视觉风格定义（参考 ChatGPT 网页版）

### 5.1 整体风格
- **简洁现代**：大面积留白、干净的线条、低饱和度配色
- **深色/浅色双主题**：默认浅色，支持切换深色模式
- **沉浸式体验**：减少视觉干扰，内容居中展示

### 5.2 配色方案

**浅色模式**：
| 元素 | 颜色 |
|------|------|
| 背景 | `#F9F9F9`（浅灰） |
| 侧边栏背景 | `#F0F0F0` |
| 用户消息气泡 | `#E3F2FD`（浅蓝）或 `#F5F5F5` |
| AI 回复气泡 | `#FFFFFF`（白色，带浅阴影） |
| 主按钮 | `#10A37F`（品牌绿） |
| 文字主色 | `#1A1A1A` |
| 文字副色 | `#666666` |
| 分割线 | `#E5E5E5` |

**深色模式**：
| 元素 | 颜色 |
|------|------|
| 背景 | `#212121` |
| 侧边栏背景 | `#171717` |
| 用户消息气泡 | `#2F2F2F` |
| AI 回复气泡 | `#424242` |
| 主按钮 | `#19C37D` |
| 文字主色 | `#ECECEC` |
| 文字副色 | `#B0B0B0` |
| 分割线 | `#333333` |

### 5.3 排版
- **字体**：系统默认无衬线字体（`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto`）
- **字号**：
  - 标题：16-18px
  - 正文/对话内容：15px
  - 辅助信息：12-13px
- **行高**：1.5-1.6
- **内容最大宽度**：768px（对话区域居中）

### 5.4 组件风格
- **圆角**：统一 8-12px
- **阴影**：轻微阴影 `0 2px 8px rgba(0,0,0,0.08)`
- **按钮**：圆角矩形，hover 有颜色加深过渡
- **输入框**：圆角矩形，focus 时有浅色边框高亮
- **气泡**：圆角矩形，带轻微阴影区分层级

### 5.5 动效
- **页面切换**：淡入淡出，200ms
- **消息出现**：从底部轻微上滑进入，200ms
- **按钮 hover**：颜色/阴影平滑过渡，150ms
- **加载动画**：旋转圆点/脉冲呼吸效果
- **拖拽上传**：遮罩淡入 + 区域边框高亮

### 5.6 图标风格
- 使用 **SVG 线性图标**（简洁、现代）
- 推荐图标库：Lucide Icons 或 Heroicons
- 图标大小：16-24px

---

## 6. 技术栈建议

### 后端
| 技术 | 用途 |
|------|------|
| FastAPI (Python) | 后端框架 |
| SQLite / JSON 文件 | 数据存储 |
| 异步任务 (asyncio) | 轮询 AI API |

### 前端
| 技术 | 用途 |
|------|------|
| Vue 3 / React | 前端框架 |
| TailwindCSS | UI |
| Axios | 请求 |

### 目录结构
```
generate_image/
├── backend/
│   ├── main.py              # FastAPI 入口
│   ├── config.py            # 配置管理
│   ├── generate.py          # 调用 AI 生图 API
│   ├── upload_cdn.py        # 图床上传（复用已有逻辑）
│   └── db/
│       ├── image_url_mapping.csv  # 参考图映射
│       ├── prompts.json           # 提示词仓库
│       └── tasks.json             # 任务记录
├── frontend/
│   └── ...
├── uploads/                 # 用户上传的参考图
├── generated_images/        # 生成的图片
└── .env                     # 配置文件
```

---

## 7. API 接口设计

### 7.1 图像生成
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/generate/text | 提交纯文本生图任务 |
| POST | /api/generate/text-image | 提交文本+图像生图任务 |

### 7.2 图片上传
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/upload | 上传图片到图床（自动去重） |

### 7.3 任务管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/tasks/:id | 查询任务状态 |
| GET | /api/tasks | 获取任务列表 |

### 7.4 图库管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/images | 获取已生成的图片列表 |
| GET | /api/images/:id | 获取图片详情 |

### 7.5 提示词仓库
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/prompts | 获取提示词列表 |
| POST | /api/prompts | 新增提示词 |
| PUT | /api/prompts/:id | 编辑提示词 |
| DELETE | /api/prompts/:id | 删除单条提示词 |
| DELETE | /api/prompts/batch | 批量删除提示词 |
| POST | /api/prompts/import | 批量导入提示词 |
| GET | /api/prompts/export | 批量导出提示词 |

### 7.6 统计
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/stats | 获取调用统计 |
