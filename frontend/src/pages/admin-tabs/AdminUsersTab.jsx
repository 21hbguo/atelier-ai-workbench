import { Trash2, Key, UserCheck, UserX, Snowflake, Sun } from 'lucide-react'
import SearchInput from '../../components/SearchInput'
import Pagination from '../../components/Pagination'
export default function AdminUsersTab({userTotal,userQuery,setUserQuery,handleMigratePoints,loading,users,setAdjustUserId,setAdjustAmount,setAdjustDesc,resetPwdUserId,setResetPwdUserId,resetPwdValue,setResetPwdValue,handleToggleFreeze,handleDeleteUser,handleResetPassword,userPage,setUserPage}){return(<div><div className="flex items-center gap-3 mb-4"><span className="text-sm" style={{ color: 'var(--text-secondary)' }}>共 {userTotal} 个用户</span><SearchInput value={userQuery} onChange={setUserQuery} placeholder="搜索用户名/昵称..." /><button onClick={handleMigratePoints} className="ml-auto px-3 py-1.5 rounded-lg text-xs font-medium bg-accent text-white hover:opacity-90">补发积分</button></div>{loading?(<div className="flex justify-center py-20"><div className="w-8 h-8 border-2 rounded-full animate-spin-slow" style={{ borderTopColor: 'var(--accent)', borderColor: 'var(--border-color)' }} /></div>):(<><div className="rounded-xl border overflow-hidden" style={{ borderColor: 'var(--border-color)' }}><div className="overflow-x-auto"><table className="w-full text-xs"><thead><tr style={{ background: 'var(--bg-primary)' }}><th className="px-3 py-2 text-left font-medium" style={{ color: 'var(--text-secondary)' }}>用户</th><th className="px-3 py-2 text-center font-medium" style={{ color: 'var(--text-secondary)' }}>积分</th><th className="px-3 py-2 text-center font-medium" style={{ color: 'var(--text-secondary)' }}>成功</th><th className="px-3 py-2 text-center font-medium" style={{ color: 'var(--text-secondary)' }}>失败</th><th className="px-3 py-2 text-center font-medium" style={{ color: 'var(--text-secondary)' }}>处理中</th><th className="px-3 py-2 text-center font-medium" style={{ color: 'var(--text-secondary)' }}>状态</th><th className="px-3 py-2 text-center font-medium" style={{ color: 'var(--text-secondary)' }}>IP</th><th className="px-3 py-2 text-center font-medium" style={{ color: 'var(--text-secondary)' }}>最后活跃</th><th className="px-3 py-2 text-right font-medium" style={{ color: 'var(--text-secondary)' }}>操作</th></tr></thead><tbody>{users.map(u=><><tr key={u.id} className="border-t" style={{ borderColor: 'var(--border-color)' }}><td className="px-3 py-2"><div className="flex items-center gap-1.5">{u.is_admin ? <span className="px-1 py-0.5 rounded text-[10px] font-medium" style={{ background: 'var(--accent)20', color: 'var(--accent)' }}>管</span> : null}<span style={{ color: 'var(--text-primary)' }}>{u.nickname || u.username}</span></div><div className="text-xs mt-0.5" style={{ color: 'var(--text-secondary)' }}>@{u.username}</div></td><td className="px-3 py-2 text-center"><button onClick={() => { setAdjustUserId(u.id); setAdjustAmount(''); setAdjustDesc('') }} className="px-1.5 py-0.5 rounded text-xs font-medium hover:bg-black/5" style={{ color: 'var(--accent)' }}>{u.is_admin ? '∞' : (u.points ?? 0)}</button></td><td className="px-3 py-2 text-center" style={{ color: '#22c55e' }}>{u.success_count}</td><td className="px-3 py-2 text-center" style={{ color: '#ef4444' }}>{u.failed_count}</td><td className="px-3 py-2 text-center" style={{ color: '#f59e0b' }}>{u.processing_count}</td><td className="px-3 py-2 text-center">{u.is_frozen ? <UserX size={12} className="inline" style={{ color: '#ef4444' }} /> : <UserCheck size={12} className="inline" style={{ color: '#22c55e' }} />}</td><td className="px-3 py-2 text-center" style={{ color: 'var(--text-secondary)' }}>{u.last_ip || '-'}</td><td className="px-3 py-2 text-center" style={{ color: 'var(--text-secondary)' }}>{u.last_active || '-'}</td><td className="px-3 py-2 text-right">{!u.is_admin && (<div className="flex items-center justify-end gap-1"><button onClick={() => { setResetPwdUserId(resetPwdUserId === u.id ? null : u.id); setResetPwdValue('') }} className="p-1.5 rounded-lg hover:bg-black/5" style={{ color: resetPwdUserId === u.id ? 'var(--accent)' : 'var(--text-secondary)' }} title="重置密码"><Key size={14} /></button><button onClick={() => handleToggleFreeze(u.id, u.username)} className="p-1.5 rounded-lg hover:bg-black/5" style={{ color: u.is_frozen ? '#3b82f6' : 'var(--text-secondary)' }} title={u.is_frozen ? '启用' : '冻结'}>{u.is_frozen ? <Sun size={14} /> : <Snowflake size={14} />}</button><button onClick={() => handleDeleteUser(u.id, u.username)} className="p-1.5 rounded-lg hover:bg-red-50 dark:hover:bg-red-900/20 text-red-500" title="删除"><Trash2 size={14} /></button></div>)}</td></tr>{resetPwdUserId===u.id&&(<tr><td colSpan="10" className="px-3 py-2" style={{ background: 'var(--bg-primary)' }}><div className="flex items-center gap-2"><span className="text-xs" style={{ color: 'var(--text-secondary)' }}>新密码</span><input type="text" value={resetPwdValue} onChange={e => setResetPwdValue(e.target.value)} placeholder="至少6位" className="w-32 px-2 py-1 rounded text-xs border outline-none" style={{ borderColor: 'var(--border-color)', background: 'var(--bg-secondary)', color: 'var(--text-primary)' }} /><button onClick={() => handleResetPassword(u.id)} disabled={!resetPwdValue.trim() || resetPwdValue.length < 6} className="px-2 py-1 rounded text-xs font-medium text-white disabled:opacity-50" style={{ background: 'var(--accent)' }}>确认</button><button onClick={() => setResetPwdUserId(null)} className="px-2 py-1 rounded text-xs" style={{ color: 'var(--text-secondary)' }}>取消</button></div></td></tr>)}</>)}</tbody></table></div></div><Pagination page={userPage} totalPages={Math.ceil(userTotal / 20)} onPageChange={setUserPage} /></>)}</div>)}
