# AI 图像生成平台数据库高并发改造方案
## 一、目标与范围
- 目标：从当前 SQLite 单机方案升级为支持百/千/万并发增长路径的分布式架构，满足多实例部署、在线扩容、故障自动切换。
- 适用模块：用户鉴权、积分、任务、广场、提示词、统计、公告、图片元数据。
- 里程碑目标（分阶段）：

| 阶段 | 注册用户规模 | 峰值在线 | API 峰值 QPS | 数据库写入 TPS | 可用性目标 |
|---|---:|---:|---:|---:|---:|
| P1 | 100万 | 1万 | 3,000 | 1,000 | 99.9% |
| P2 | 500万 | 10万 | 20,000 | 6,000 | 99.95% |
| P3 | 1000万+ | 30万+ | 50,000+ | 15,000+ | 99.99% |

## 二、现状瓶颈（基于当前代码）
- 当前数据库为 `backend/database.py` 中的 SQLite 文件库 `data/app.db`，单写锁导致高并发写入阻塞。
- `TaskManager` 同时依赖进程内 `_tasks` 和 SQLite，应用多实例后任务状态不一致。
- 登录/注册限流在内存字典（`backend/routers/auth.py`），多实例下限流失效。
- 统计表 `stats` 为单行热点更新（`today_requests/total_requests`），并发上来后锁冲突明显。
- 广场点赞 `square_images.likes_count` 直接行更新，高频场景易形成热点行。
- 缺少连接池代理、读写分离、分区分片和跨机房容灾能力。

## 三、总体架构设计
## 3.1 架构选型
- 事务主库：PostgreSQL 16（生产推荐托管版）
- 连接池代理：PgBouncer（事务池模式）
- 缓存与分布式能力：Redis Cluster
- 异步队列：Kafka（或 Redis Streams，P1 可先用 Redis Streams）
- 对象存储：S3/OSS/COS + CDN（图片文件不落本地磁盘）
- ORM/SQL 层：SQLAlchemy Core + Alembic（统一迁移）

## 3.2 逻辑分层
- 核心强一致库（PostgreSQL）：`users`、`point_accounts`、`point_transactions`、`redemption_codes`、`generation_tasks`。
- 高并发可最终一致（Redis + 异步回刷）：点赞计数、浏览计数、实时排行榜。
- 冷数据与归档：按月分区并迁移到低成本存储，保留在线热数据窗口。

## 3.3 服务拓扑
- `Nginx/API Gateway -> FastAPI(多副本) -> PgBouncer -> PostgreSQL Primary/Read Replicas`
- `FastAPI -> Redis Cluster(缓存/限流/分布式锁/计数)`
- `FastAPI -> Queue(Kafka/Redis Streams) -> Worker(任务轮询与回调落库)`
- `Worker -> Object Storage -> CDN`

## 四、数据模型改造
## 4.1 表拆分与重构
- `users` 拆分为 `user_profile` 与 `user_credential`（避免热点字段与敏感字段混读）。
- 积分从 `users.points` 独立到 `point_accounts(balance,version)`，流水写 `point_transactions`。
- `tasks` 重构为 `generation_tasks`（结构化字段 + JSONB 扩展字段），去除进程内状态真相源。
- `square_images` 与 `square_likes` 保留，但点赞计数改为 Redis 计数+异步回刷。
- `user_requests` 作为限流与审计日志表，按日期分区。

## 4.2 分区策略
- `point_transactions`：按 `created_at` 月分区（Range Partition）。
- `generation_tasks`：按 `created_at` 月分区，索引 `(user_id,created_at desc)`。
- `user_requests`：按天/周分区，保留 30-90 天在线。
- `square_images`：P2 起按 `user_id hash` 分区，降低热点。

## 4.3 索引策略
- 用户登录：`user_credential(username)` 唯一索引。
- 任务查询：`generation_tasks(user_id,updated_at desc)`、`generation_tasks(status,updated_at desc)`。
- 积分流水：`point_transactions(user_id,created_at desc)`。
- 点赞关系：`square_likes(image_id,user_id)` 唯一索引 + `square_likes(user_id,created_at desc)`。
- 公告已读：`announcement_reads(user_id,announcement_id)` 唯一索引。

## 五、并发一致性设计
## 5.1 积分扣减（强一致）
- 单事务流程：`SELECT ... FOR UPDATE` 锁账户行 -> 校验余额 -> 更新余额与版本号 -> 插入流水。
- 失败自动回滚，禁止“先扣后记”跨事务写法。
- 每个生成请求带 `request_id` 幂等键，防止重复扣费。

## 5.2 点赞与统计（最终一致）
- 点赞写路径：先写关系表（唯一约束防重）-> Redis `INCR/DECR` 计数。
- 异步批量回刷：每 1-5 秒聚合增量，批量更新 `square_images.likes_count`。
- 统计项（today/total）由事件流聚合，不再写单行热点 `stats`。

## 5.3 限流与防重
- 登录/注册/生成频控迁移到 Redis Lua 脚本令牌桶。
- 兑换码使用 `UPDATE ... WHERE is_used=false` 原子抢占，返回影响行数判断成功。

## 六、实施方案（落地步骤）
## 6.1 第 0 阶段：准备
- 新建 `alembic` 迁移体系，冻结现有 SQLite schema。
- 盘点所有 SQL（主要在 `backend/routers/*`、`backend/services/*`），标注读写路径。
- 建立压测基线：登录、生成、任务查询、点赞、签到、兑换码五类场景。

## 6.2 第 1 阶段：基础设施上线
- 部署 PostgreSQL Primary + 2 Read Replica。
- 部署 PgBouncer 与 Redis Cluster。
- 引入对象存储，图片上传改为存储 URL，不依赖本地 `data/images`。

## 6.3 第 2 阶段：代码改造
- 抽象 `backend/db/` 数据访问层，替代直接 `sqlite3.connect()`。
- SQL 参数化统一改为命名参数，兼容 PostgreSQL。
- `TaskManager` 改为“数据库+Redis”为唯一状态源，删除进程内 `_tasks` 真相源。
- 限流逻辑从内存字典迁移到 Redis。

## 6.4 第 3 阶段：数据迁移
- 全量迁移：SQLite 导出 -> PostgreSQL 导入（分表校验行数与校验和）。
- 增量迁移：双写期（SQLite+PostgreSQL）1-2 周，对账比对。
- 灰度切流：按用户分组切 5% -> 20% -> 50% -> 100%。

## 6.5 第 4 阶段：切换与收口
- 完成全量切换后关闭 SQLite 写入，只保留只读备份。
- 开启自动分区创建、归档任务、慢 SQL 巡检。
- 发布容量报告与故障演练报告。

## 七、关键 SQL/事务模板（PostgreSQL）
## 7.1 积分扣减模板
```sql
BEGIN;
SELECT balance FROM point_accounts WHERE user_id=:user_id FOR UPDATE;
UPDATE point_accounts SET balance=balance-:cost,version=version+1,updated_at=now() WHERE user_id=:user_id AND balance>=:cost;
INSERT INTO point_transactions(user_id,amount,type,request_id,created_at) VALUES(:user_id,:cost*-1,'generate_consume',:request_id,now());
COMMIT;
```
## 7.2 兑换码核销模板
```sql
UPDATE redemption_codes SET is_used=true,used_by=:user_id,used_at=now() WHERE code=:code AND is_used=false;
```
## 7.3 点赞防重模板
```sql
INSERT INTO square_likes(image_id,user_id,created_at) VALUES(:image_id,:user_id,now()) ON CONFLICT(image_id,user_id) DO NOTHING;
```

## 八、观测与容量治理
- 指标：连接池占用、事务冲突率、锁等待、慢 SQL、P95/P99 延迟、Redis 命中率、队列堆积长度。
- 告警阈值：

| 指标 | 阈值 |
|---|---|
| PostgreSQL CPU | >70% 持续 10 分钟 |
| 锁等待 | >200ms P95 |
| API P99 | >800ms |
| Redis 命中率 | <85% |
| 队列积压 | >5 分钟未消费 |

- 压测目标（切换前必须达成）：

| 场景 | 目标 |
|---|---|
| 登录/鉴权 | 5,000 RPS，错误率 <0.1% |
| 任务查询 | 20,000 RPS，P95 <150ms |
| 点赞写入 | 10,000 RPS，无重复点赞脏写 |
| 积分扣减 | 3,000 TPS，零负余额 |

## 九、风险与回滚
- 风险：双写不一致、热点分区倾斜、缓存雪崩、DDL 锁表。
- 控制：幂等键、分批 DDL、限流熔断、预热缓存、只增不删迁移策略。
- 回滚：保留双写开关与读路由开关，异常时 5 分钟内切回旧读路径；数据以 PostgreSQL 为主账，SQLite 仅作为应急只读备份。

## 十、与当前项目对应的改造清单
- 新增：`backend/db/engine.py`、`backend/db/session.py`、`backend/db/repositories/*`、`backend/alembic/*`。
- 修改：`backend/database.py`（降级为兼容层或删除）、`backend/services/task_manager.py`、`backend/routers/auth.py`、`backend/routers/generate.py`、`backend/services/points_service.py`、`backend/services/stats_service.py`。
- 配置：`.env` 增加 `DATABASE_URL`、`READ_DATABASE_URL`、`REDIS_URL`、`QUEUE_URL`、`PGBOUNCER_URL`。

## 十一、预计排期（6-10 周）
| 周期 | 产出 |
|---|---|
| 第1周 | 架构评审、SQL 盘点、压测基线 |
| 第2-3周 | PostgreSQL/Redis/队列基础设施落地 |
| 第4-6周 | 代码改造、迁移脚本、灰度开关 |
| 第7-8周 | 双写对账、分批灰度、压测复测 |
| 第9-10周 | 全量切换、回收旧链路、稳定性观察 |
