# 项目安全加固建议

## 高优先级

### 1. 关闭任意本地路径缩略图读取
问题位置：[PROJECT_ROOT/backend/routers/images.py:171](PROJECT_ROOT/backend/routers/images.py:171)

`/api/images/local-thumb` 当前没有登录校验，而且允许传入任意 `path`。只要服务器上的该路径存在且是图片文件，就可以被读取。

建议：
- 立刻加 `Depends(require_admin)` 或至少 `Depends(get_current_user)`
- 不再接收任意绝对路径，只允许读取 `UPLOAD_DIR` 或 `GENERATED_IMAGES_DIR` 下的文件
- 最好改成前端传 `id` 或 `filename`，后端自己查路径

### 2. 限制 `/upload/local` 文件类型
问题位置：[PROJECT_ROOT/backend/routers/upload.py:83](PROJECT_ROOT/backend/routers/upload.py:83)
静态暴露位置：[PROJECT_ROOT/backend/main.py:61](PROJECT_ROOT/backend/main.py:61)

本地上传接口没有校验扩展名，上传后的文件又会通过 `/uploads` 直接对外提供。

风险：
- 可上传 `svg`、`html` 等文件，形成存储型 XSS 或钓鱼页面
- 任意文件会长期暴露在公网路径下

建议：
- `upload_local` 只允许 `png`、`jpg`、`jpeg`、`webp`、`pdf`
- 同时校验 `content-type` 和文件头，不只看后缀
- `/uploads` 不再直接静态公开，支付凭证这类文件改成鉴权下载

### 3. 不要信任客户端传来的 `X-Forwarded-For`
问题位置：[PROJECT_ROOT/backend/auth.py:123](PROJECT_ROOT/backend/auth.py:123)

当前逻辑会直接读取请求头中的 `X-Forwarded-For`。任何客户端都可以伪造这个头，导致登录、注册、兑换码、生成任务的频率限制被绕过。

建议：
- 只有在明确经过受信任 Nginx 反向代理时才读取该头
- 默认使用 `request.client.host`
- 由 Nginx 负责清洗并覆盖转发头，后端只按受信代理模式解析

### 4. 缩短 Token 生命周期，不再放在 `localStorage`
问题位置：[PROJECT_ROOT/backend/auth.py:43](PROJECT_ROOT/backend/auth.py:43)
前端位置：[PROJECT_ROOT/frontend/src/api.js:9](PROJECT_ROOT/frontend/src/api.js:9)

JWT 默认有效期为 720 小时，前端又把 token 存进了 `localStorage`。一旦存在 XSS，攻击者可以长时间控制账号。

建议：
- `access token` 改成 2 到 12 小时
- 单独增加 `refresh token`
- 前端改用 `HttpOnly`、`Secure`、`SameSite=Lax/Strict` Cookie

## 中优先级


### 6. 强化响应头和 HTTPS
问题位置：[PROJECT_ROOT/backend/main.py:36](PROJECT_ROOT/backend/main.py:36)

当前只加了少量基础安全响应头，防护还不够完整。

建议：
- 增加 `Content-Security-Policy`
- 上 HTTPS 后增加 `Strict-Transport-Security`
- 增加 `Permissions-Policy`
- 静态资源按需设置 `Cache-Control`

### 7. 收紧配置接口暴露范围
问题位置：[PROJECT_ROOT/backend/routers/config.py:21](PROJECT_ROOT/backend/routers/config.py:21)

当前任意登录用户都可以读取运行时配置。虽然 `api_key` 被打码，但第三方服务地址、支付二维码地址等信息仍然会暴露。

建议：
- 普通用户只返回前端必需字段
- 管理配置拆成单独的管理员读取接口

## 部署层建议

### 8. 在 Nginx 层补一层限制
文件位置：[PROJECT_ROOT/nginx.conf](PROJECT_ROOT/nginx.conf)

建议：
- 对 `/api/auth/login`、`/api/auth/register` 增加 `limit_req`
- 限制请求体大小，例如 `client_max_body_size 10m`
- 禁止访问危险类型的上传文件
- 生产环境只开放 `80/443`，不要暴露开发端口

## 推荐执行顺序

1. 修复 `local-thumb` 越权与任意路径读取
2. 修复 `upload/local` 文件类型与公开访问
3. 修复 IP 信任链
4. 缩短 JWT 生命周期并改为 Cookie
5. 为外部下载增加白名单和大小限制

## 建议优先落地的改动

1. 封掉 `local-thumb` 的任意路径读取
2. 收紧 `upload/local`
3. 修复 `get_client_ip`
4. 补一版基础安全响应头和 Nginx 限流
