# 项目问题清单
## P0
1. 注册流程事务嵌套导致数据库锁冲突
- 位置：`backend/routers/auth.py:61`、`backend/services/points_service.py:65`
- 现象：注册时同一请求内多连接写 SQLite，触发 `sqlite3.OperationalError: database is locked`
- 影响：新用户注册失败，核心功能阻断
2. 可选鉴权实现错误导致公开接口被强制登录
- 位置：`backend/auth.py:29`、`backend/auth.py:76`、`backend/routers/prompts.py:66`、`backend/routers/square.py:39`
- 现象：`HTTPBearer` 默认 `auto_error=True`，匿名访问被 403 拦截
- 影响：公开内容页不可匿名访问，前台可用性严重下降
3. 用户统计接口泄露敏感信息
- 位置：`backend/routers/stats.py:80`
- 现象：普通登录用户可获取全量用户 `last_ip`、`last_active`
- 影响：隐私泄露与合规风险
4. 图片元数据可被越权篡改并联动越权删除
- 位置：`backend/routers/images.py:230`、`backend/routers/images.py:213`
- 现象：任意登录用户可覆盖 `filename` 对应 `user_id`，再通过删除权限校验删除他人图片
- 影响：越权写+越权删，安全风险高
## P1
5. 任务状态以内存为主，多实例部署状态不一致
- 位置：`backend/services/task_manager.py:12`、`backend/services/task_manager.py:109`、`backend/services/task_manager.py:113`
- 现象：任务真相源在进程内 `_tasks`，跨副本查询可能查不到
- 影响：扩容后任务查询和管理不可靠
6. 生成接口采用同步长轮询阻塞模型
- 位置：`backend/routers/generate.py:81`、`backend/routers/generate.py:151`、`backend/routers/generate.py:178`
- 现象：单请求占用连接等待外部任务完成
- 影响：并发能力弱，峰值时吞吐明显下降
7. 兑换码核销存在并发竞争窗口
- 位置：`backend/services/points_service.py:110`
- 现象：先查 `is_used` 再更新，更新条件未带 `is_used=0`
- 影响：并发下可能重复兑换
8. JWT 密钥默认本地文件生成，不适合多实例
- 位置：`backend/auth.py:13`
- 现象：未统一配置 `JWT_SECRET` 时各实例密钥不一致
- 影响：跨实例 token 失效、用户随机掉线
## P2
9. 存在明显 N+1 与目录全扫描性能问题
- 位置：`backend/services/prompt_service.py:63`、`backend/services/prompt_service.py:184`、`backend/routers/images.py:61`
- 现象：列表查询按条补查点赞状态，图片列表遍历全目录
- 影响：数据量增长后响应时间和 CPU 升高
## 建议修复顺序
1. 修复注册锁冲突与事务边界
2. 修复可选鉴权（公开接口匿名可用）
3. 收紧统计接口权限与字段脱敏
4. 封堵图片元数据越权链路
5. 任务状态源改为数据库/缓存统一真相源
6. 生成流程改为异步提交+异步查询
7. 兑换码改原子更新语义
8. 强制生产环境配置统一 JWT 密钥
9. 批量优化 N+1 与目录扫描热点
## 错误教训
10. 数字状态直接参与 JSX 条件渲染会把 `0` 渲染到页面
- 位置：`frontend/src/components/Sidebar.jsx`、`frontend/src/pages/ChatPage.jsx`、`frontend/src/pages/AdminPage.jsx`、`frontend/src/pages/AnnouncementsPage.jsx`
- 现象：侧边栏、缩略图区、失败状态区、公告区、导出相关页面出现空白行或孤立 `0`
- 根因：后端返回 `is_admin/is_frozen/is_read` 为 `0/1`，前端使用 `x && <Element />` 时 `x=0` 会被 React 当作文本节点渲染
- 教训：布尔语义字段必须在后端统一转 `bool` 输出，前端统一 `Boolean(x)` 后再条件渲染，禁止数字/长度值直接作为 JSX 条件值
