# AI 图像生成平台 — 积分消耗系统设计文档

## 一、需求概述

为 AI 图像生成应用添加积分系统，控制用户生成次数，支持多种积分获取方式。

### 核心功能

| 功能 | 说明 |
|------|------|
| 生成消耗 | 每次请求消耗 10 积分，失败时自动退还 |
| 每日签到 | 每天签到获得 10 积分 |
| 注册赠送 | 新用户注册获得 50 积分 |
| 兑换码 | 管理员生成兑换码，用户输入兑换码获得积分 |
| 管理员免积分 | is_admin 用户不受积分限制 |
| 补发积分 | 给所有现有用户一次性补发 50 积分 |

### UI 展示

- 侧边栏用户信息区显示积分余额 + 签到按钮
- 首页工具栏右侧显示积分余额
- 生成成功/失败时显示积分变动提示
- 管理后台用户列表显示积分余额
- 管理后台新增「兑换码管理」Tab

---

## 二、数据库设计

### 2.1 修改 users 表

```sql
ALTER TABLE users ADD COLUMN points INTEGER DEFAULT 0;
```

### 2.2 新建 redemption_codes（兑换码表）

```sql
CREATE TABLE IF NOT EXISTS redemption_codes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code TEXT UNIQUE NOT NULL,
    points INTEGER NOT NULL,
    is_used INTEGER DEFAULT 0,
    used_by INTEGER,
    used_by_ip TEXT,
    used_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (used_by) REFERENCES users(id)
);
CREATE INDEX IF NOT EXISTS idx_redemption_codes_code ON redemption_codes(code);
CREATE INDEX IF NOT EXISTS idx_redemption_codes_is_used ON redemption_codes(is_used);
```

### 2.3 新建 point_transactions（积分流水表）

```sql
CREATE TABLE IF NOT EXISTS point_transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    amount INTEGER NOT NULL,
    balance_after INTEGER NOT NULL,
    type TEXT NOT NULL,
    description TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
CREATE INDEX IF NOT EXISTS idx_point_tx_user_id ON point_transactions(user_id);
CREATE INDEX IF NOT EXISTS idx_point_tx_created_at ON point_transactions(created_at DESC);
```

**type 字段值**：`register_bonus`、`daily_checkin`、`generate_consume`、`generate_refund`、`redeem_code`、`admin_grant`、`migration_bonus`

### 2.4 新建 daily_checkins（签到记录表）

```sql
CREATE TABLE IF NOT EXISTS daily_checkins (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    checkin_date TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, checkin_date)
);
CREATE INDEX IF NOT EXISTS idx_daily_checkins_user_date ON daily_checkins(user_id, checkin_date);
```

---

## 三、后端 API 设计

### 3.1 积分路由（新增）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/points/balance` | 查询当前用户积分余额 |
| POST | `/api/points/checkin` | 每日签到，+10 积分 |
| POST | `/api/points/redeem` | 兑换码兑换，Body: `{"code": "XXX"}` |
| GET | `/api/points/transactions` | 积分流水，分页查询 |

### 3.2 管理员路由（扩展）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/codes` | 兑换码列表（分页+排序） |
| POST | `/api/admin/codes` | 生成兑换码，Body: `{"count": N, "points": M}` |
| POST | `/api/admin/users/{id}/points` | 手动调整用户积分 |
| POST | `/api/admin/migrate-points` | 补发积分：所有现有用户 +50 |

### 3.3 已有路由修改

| 路由 | 修改内容 |
|------|---------|
| `POST /api/auth/register` | 注册时赠送 50 积分 |
| `POST /api/auth/login` | 响应包含 points 字段 |
| `GET /api/auth/me` | 响应包含 points 字段 |
| `POST /api/generate/text` | 生成前扣除 10 积分，失败时退还 |
| `POST /api/generate/text-image` | 同上 |

---

## 四、前端设计

### 4.1 新增页面

- **RedeemPage** (`/redeem`) — 兑换码输入页

### 4.2 修改组件

| 组件 | 修改内容 |
|------|---------|
| `api.js` | 新增 `pointsAPI`，扩展 `adminAPI` |
| `Sidebar.jsx` | 用户信息区显示积分余额 + 签到按钮 |
| `ChatPage.jsx` | 工具栏显示积分，402 错误提示，生成变动提示 |
| `AdminPage.jsx` | 新增「兑换码管理」Tab，用户列表增加积分列 |
| `App.jsx` | 新增 `/redeem` 路由 |

---

## 五、关键实现细节

### 5.1 并发安全

积分扣除使用原子 SQL：
```sql
UPDATE users SET points = points - 10 WHERE id = ? AND points >= 10
```
检查 `cursor.rowcount`，为 0 则表示积分不足。

### 5.2 管理员跳过

在 PointsService 的 `has_enough()` 中先检查 `is_admin`，管理员直接返回 True，不产生扣除记录。

### 5.3 积分变动同步

每次积分变动后返回新余额，前端更新 localStorage 中的 user 对象，并通过 `window.dispatchEvent(new Event('points-updated'))` 通知其他组件刷新。

### 5.4 HTTP 状态码

积分不足返回 **402 Payment Required**，前端可精确识别并提示。

---

## 六、实现步骤

1. database.py — 建表 + 加列
2. points_service.py — 核心服务
3. routers/auth.py — 注册赠送 + 登录/用户信息返回积分
4. routers/generate.py — 扣除 + 退还
5. routers/points.py — 积分路由
6. routers/admin.py — 兑换码管理 + 用户积分 + 补发
7. main.py — 注册路由
8. api.js — 前端 API
9. Sidebar.jsx — 积分显示 + 签到
10. ChatPage.jsx — 积分提示
11. RedeemPage.jsx + App.jsx — 兑换页面
12. AdminPage.jsx — 管理后台

---

## 七、文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/database.py` | 修改 | 建表 + 加列 |
| `backend/services/points_service.py` | 新增 | 积分核心服务 |
| `backend/routers/points.py` | 新增 | 积分路由 |
| `backend/routers/generate.py` | 修改 | 积分扣除 + 退还 |
| `backend/routers/auth.py` | 修改 | 注册赠送 + 返回积分 |
| `backend/routers/admin.py` | 修改 | 兑换码管理 + 补发 |
| `backend/main.py` | 修改 | 注册路由 |
| `backend/models/schemas.py` | 修改 | 新增 RedeemRequest |
| `frontend/src/api.js` | 修改 | 新增 pointsAPI |
| `frontend/src/components/Sidebar.jsx` | 修改 | 积分显示 + 签到 |
| `frontend/src/pages/ChatPage.jsx` | 修改 | 积分提示 |
| `frontend/src/pages/RedeemPage.jsx` | 新增 | 兑换码页面 |
| `frontend/src/pages/AdminPage.jsx` | 修改 | 兑换码管理 Tab |
| `frontend/src/App.jsx` | 修改 | 新增路由 |

---

## 八、验证清单

- [ ] 注册新用户 → 积分余额 50
- [ ] 签到 → +10，重复签到被拒
- [ ] 生成图片 → -10；失败 → 退还 +10
- [ ] 管理员生成 → 余额不变
- [ ] 兑换码兑换 → 余额增加
- [ ] 管理后台生成兑换码 → 列表正确
- [ ] 补发积分 → 所有现有用户 +50
- [ ] 积分不足时生成 → 返回 402 提示
